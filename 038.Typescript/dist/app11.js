//Создайте переменную str и присвойте ей строковое значение. Используя цикл for выведите каждый символ строки и его индекс.
const p = "hello";
for (let i = 0; i < p.length; i++) {
    console.log(p[i] + " " + i);
}
