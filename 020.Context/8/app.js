// Напишите функцию, принимающую массив в качестве параметра.
// Необходимо оставить в массиве символы без дубликатов. IIFE [1, 1, 2, 2, 2, 3, 4, 5, 5, 5]= [1, 2, 3, 4, 5]

(function idValue(id) {
  let arr = [];
  for (let i = 0; i < id.length; i++) {
    if (!arr.includes(id[i])) {
      arr.push(id[i]);
    }
  }
  console.log(arr);
})([1, 1, 2, 2, 2, 3, 4, 5, 5, 5]);
